import { TestBed } from '@angular/core/testing';

import { {{upperName}}Module } from './{{inputName}}.module';

describe('{{upperName}}Module', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      imports: [{{upperName}}Module],
    });
  });

  it('initializes', () => {
    const module: {{upperName}}Module = TestBed.inject({{upperName}}Module);
    expect(module).toBeTruthy();
  });
});
